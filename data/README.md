# Readings Data

`readings.json` is the local database for reference or generated dashboard interpretations.

The app still saves in-progress user readings to the browser, while this file holds reusable readings that should travel with the repository.
